# Local development — your backend running on localhost
VITE_API_URL=http://localhost:5000/api

# After deploying the backend (Render, Railway, etc.), point this at its
# public URL instead, e.g.:
# VITE_API_URL=https://aura-backend.onrender.com/api
